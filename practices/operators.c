#include <stdio.h>
int main()
{
    int a = 1, b = 1;

    printf("a==b=%d ", a == b);
    printf("a!=b=%d ", a != b);
    printf("a||b=%d ", a || b);
    printf("a^b=%d ", a ^ b);
    printf("a&&b=%d ", a && b);
    return 0;
}